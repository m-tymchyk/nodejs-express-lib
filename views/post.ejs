<!DOCTYPE html>
<html lang="en">

  <head>

    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Clean Blog - Start Bootstrap Theme</title>

    <!-- Bootstrap core CSS -->
    <link rel="stylesheet" href="../bootstrap/css/bootstrap.min.css">

    <!-- Custom fonts for this template -->
    <link href="../vendor/font-awesome/css/font-awesome.min.css" rel="stylesheet">
    <link href='https://fonts.googleapis.com/css?family=Lora:400,700,400italic,700italic' rel='stylesheet'>
    <link href='https://fonts.googleapis.com/css?family=Open+Sans:300italic,400italic,600italic,700italic,800italic,400,300,600,700,800' rel='stylesheet'>

    <!-- Custom styles for this template -->
    <link href="../css/clean-blog.min.css" rel="stylesheet" type="text/css">

  </head>

  <body>

    <!-- Navigation -->

    <!-- Page Header -->

    <!-- Post Content -->
    <article>
      <div class="container">
        <div class="row">
          <div class="col-lg-8 col-md-10 mx-auto">
            

            <h2 id="title">The Final Frontier</h2>

            <p id="content"></p>

            <p id="topic"></p>

            <p id="rating"></p>

            <p id="hash-post"></p>
          </div>
        </div>
      </div>
    </article>

    <hr>

    <!-- Footer -->
    
    <!-- Bootstrap core JavaScript -->
    <script src="../vendor/jquery/jquery.min.js" type="application/javascript"></script>
    <script src="../vendor/bootstrap/js/bootstrap.bundle.min.js" type="text/html"></script>

    <!-- Custom scripts for this template -->
    <script src="../js/clean-blog.min.js" type="text/html"></script>
    <script>
        var item = window.location.pathname.substr(10.100);
        var info;
        $.ajax({
              type: "GET",
              url: "http://localhost:3012/posts/"+item,
              dataType: "json",
              contentType: "application/json;"
          }).done(function (data) {
              console.log(data);
              console.log(data.content);
              $('#title').text(data.title);
              $('#content').text(data.content);
              $('#topic').text('topic: ' + data.topic);
              $('#rating').text('rating: ' + data.rating);
              $('#hash-post').text("hash of post: " + data.hash_post);
        });
        
      </script>
  </body>
  

</html>
